# compression
llmlingua>=0.2.2

# preservation score
sentence-transformers>=3.0.0

# file conversion (incluye audio, OCR, etc.)
markitdown[all]>=0.1.0

# api (models_server expone su propio FastAPI app)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.9
slowapi>=0.1.9

# token counting
tiktoken>=0.7.0

# http
httpx>=0.27.0

# interactive cli (si el CLI corre también desde este container)
questionary>=2.0.0

# config
# configparser is stdlib in Python 3.10+
